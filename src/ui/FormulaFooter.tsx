/**
 * V7 页脚公式说明（F-09）：内容与 F-03/F-08/F-11 实际计算口径逐条一致
 */
export function FormulaFooter() {
  return (
    <details className="jp-footer">
      <summary>打分口径与公式说明</summary>
      <ul>
        <li>报录比 = 报名人数 ÷ 录用人数</li>
        <li>
          总分 =（平均收入 + 到手收入 + 收入打分 + 到手收入打分）÷ 报录比；展示保留两位小数（0.9348 → 0.93）
        </li>
        <li>
          收入打分默认 = 年收入万数四舍五入取整（40 万 = 40 分，49.88 万 → 50 分）；到手收入打分同规则；行内可手动改为任意数值
        </li>
        <li>报名人数或录用人数为 0/空时报录比无意义，总分显示「—」，不按 0 计算；恢复有效数值后即时重算</li>
        <li>报名人数空值按 0 参与计算（列表黄底提示），可在行内点击更新（公考雷达查询后手动填入）</li>
        <li>
          考生画像强过滤：任一轮次名单 = 链式基础名单（初筛 = 全量、后续轮 = 上一轮晋级者）∩ 画像过滤
          （学历要求等级 ≤ 考生学历 且 考区 ∈ 意向考区，两条件独立生效）
        </li>
      </ul>
    </details>
  );
}
